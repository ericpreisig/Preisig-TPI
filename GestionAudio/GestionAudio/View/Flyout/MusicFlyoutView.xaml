<UserControl x:Class="Presentation.View.Flyout.MusicFlyoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Presentation.View.Flyout"
             xmlns:list="clr-namespace:Presentation.View.List"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding Type}" Foreground="#3498db" FontSize="30" />
            <ContentControl Grid.Row="1" Grid.ColumnSpan="3" Content="{Binding MusicFlyoutView}"></ContentControl>
        </Grid>
    </Grid>
</UserControl>